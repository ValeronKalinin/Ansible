---
- hosts: all
  remote_user: ansible
  become: yes 
  gather_facts: no
  tasks:
  #обновляем систему
  - name: Upgrade System
    yum:
      name: "*"
      state: latest
  #устанавливаем минимальный набор утилит
  - name: install utils
    yum:
      name: 
        - mc
        - wget
        - net-tools
        - nano
      state: latest 
      use_backend: yum4

  - name: iinstall docker
    yum:
      name: 
        - yum-utils
        - device-mapper-persistent-data
        - lvm2
      state: latest
  
  - name: Add Docker repo
    get_url:
      url: https://download.docker.com/linux/centos/docker-ce.repo
      dest: /etc/yum.repos.d/docker-ce.repo
  - name: Install DOCKER
    yum:
      name: 
        - docker-ce 
        - docker-ce-cli 
        - containerd.io
      state: latest
  
  - name: Start Docker service
    service:
      name: docker
      state: started
      enabled: yes  
  
  - name: Testing
    command: docker run hello-world
    register: output

  - debug:
<<<<<<< HEAD
      var:  output
=======
      var: output 
>>>>>>> a731d659d38f6db7b6f4a1d0ca625ff9b85a8fdb
